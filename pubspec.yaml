name: expense_tracker
description: Personal BCA credit card expense tracker
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5

  # Navigation
  go_router: ^14.2.7

  # HTTP client
  dio: ^5.4.3+1

  # Secure storage for JWT
  flutter_secure_storage: ^9.2.2

  # Charts
  fl_chart: ^0.68.0

  # Formatting
  intl: ^0.19.0

  # Loading skeletons
  shimmer: ^3.0.0

  # Swipe actions on lists
  flutter_slidable: ^3.1.1

  # Color picker for categories
  flex_color_picker: ^3.5.1

  # Home screen widget (WidgetKit) bridge
  home_widget: ^0.7.0

  # Local push notifications — OS-level scheduling (fires even when app is killed)
  flutter_local_notifications: ^18.0.0

  # Timezone support for scheduling at 22:00 WIB (Asia/Jakarta)
  timezone: ^0.9.4

  # Background task runner — keeps notification data fresh without opening the app
  workmanager: ^0.5.2

  # Icons (extending Material icons)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.11
  riverpod_generator: ^2.4.3
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true

  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: assets/fonts/PlusJakartaSans-Regular.ttf
        - asset: assets/fonts/PlusJakartaSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/PlusJakartaSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/PlusJakartaSans-Bold.ttf
          weight: 700

  assets:
    - assets/fonts/
